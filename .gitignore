/bin/
/duckserver
internal/web/static/app.css
.env
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.backup
infra/*.tfvars
issues/
# Added by goreleaser init:
dist/
# The `make duck` / `make install` build artifact. Anchored to the repo root
# so it does not also ignore the cmd/duck source directory (an unanchored
# `duck` matches any path component named "duck").
/duck
